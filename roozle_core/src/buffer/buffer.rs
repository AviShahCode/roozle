struct Buffer {
}

impl Buffer {
    fn new() -> Buffer {
        Buffer {}
    }

    fn from_file(file_name: &str) -> Buffer {
        Buffer {}
    }

    fn from_string(string: &str) -> Buffer {
        Buffer {}
    }
}